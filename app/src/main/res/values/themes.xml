<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DogApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="materialButtonStyle">@style/MyButtonStyle</item>

    </style>

    <style name="Theme.DogApp" parent="Base.Theme.DogApp" />

    <style name="MyButtonStyle" parent="Theme.AppCompat">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/curved_button</item>
    </style>

</resources >